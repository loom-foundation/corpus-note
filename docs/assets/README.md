# Assets

`ada-lovelace-note-g.png` is the 1024x1024 artwork depicting Ada Lovelace's Note G, the first published computer program.
The docs site's hero image, `ada-lovelace-note-g.webp`, is a 78 KB WebP derived from this master for web delivery.
This master is the source; regenerate the WebP from it rather than editing the WebP in place.
